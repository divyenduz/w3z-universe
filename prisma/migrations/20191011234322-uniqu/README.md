# Migration `20191011234322-uniqu`

This migration has been generated by Divyendu Singh at 10/11/2019, 11:43:22 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX "lift"."links.url" ON "links"("url")

CREATE UNIQUE INDEX "lift"."links.converted_url" ON "links"("converted_url")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191011225648-init..20191011234322-uniqu
--- datamodel.dml
+++ datamodel.dml
@@ -8,7 +8,7 @@
 }
 model links {
   id            Int    @id
-  url           String
-  converted_url String
+  url           String @unique
+  converted_url String @unique
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191011234322-uniqu)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191011234322-uniqu'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
