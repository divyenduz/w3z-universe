# Migration `20191011225648-init`

This migration has been generated by Divyendu Singh at 10/11/2019, 10:56:48 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."links" (
  "converted_url" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "url" TEXT NOT NULL DEFAULT ''  
);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191011225648-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,14 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model links {
+  id            Int    @id
+  url           String
+  converted_url String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191011225648-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191011225648-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
