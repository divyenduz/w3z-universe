datasource db {
  provider = "sqlite"
  url      = "file:dev.db"
}

generator photon {
  provider = "photonjs"
}

model links {
  id            Int    @id
  url           String
  converted_url String
}