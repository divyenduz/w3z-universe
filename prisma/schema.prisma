datasource db {
  provider = "sqlite"
  url      = "file:dev.db"
}

generator photon {
  provider      = "photonjs"
  binaryTargets = ["darwin", "linux-glibc-libssl1.0.1"]
}

model links {
  id            Int    @id
  url           String @unique
  converted_url String @unique
}